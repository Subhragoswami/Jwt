   {
            "errorCode": "7032",
            "errorMessage": "Unexpected character ('+' (code 43)) in numeric value: JSON spec does not allow numbers to have plus signs: enable `JsonReadFeature.ALLOW_LEADING_PLUS_SIGN_FOR_NUMBERS` to allow\n at [Source: REDACTED (`StreamReadFeature.INCLUDE_SOURCE_IN_LOCATION` disabled); line: 1, column: 2]"
        }
--liquibase formatted sql
--changeset Hrishikesh:1

CREATE TABLE HELP_SUPPORT_TEAM (
    ID RAW(16) DEFAULT SYS_GUID() PRIMARY KEY,
    MID VARCHAR2(50 CHAR),
    AGGREGATOR_ID	VARCHAR2(50 CHAR),
    TYPE	VARCHAR2(20 CHAR),
    VALUE	VARCHAR2(320 CHAR),
    STATUS	VARCHAR2(20 CHAR),
    CREATED_BY	VARCHAR2(50 CHAR),
    CREATED_AT	NUMBER,
    UPDATED_BY	VARCHAR2(50 CHAR),
    UPDATED_AT	NUMBER
);

CREATE INDEX IDX_HELP_SUPPORT_TEAM_MID ON HELP_SUPPORT_TEAM(MID);
CREATE INDEX IDX_TYPE ON HELP_SUPPORT_TEAM(TYPE);

INSERT INTO HELP_SUPPORT_TEAM(TYPE, VALUE, STATUS, CREATED_BY, CREATED_AT)
VALUES('PHONE_NUMBER', '+91-22-20876156', 'Active', 'SBI ePay',
(SELECT ROUND(((TRUNC(SYSTIMESTAMP,'MI')-DATE '1970-01-01') * 24 * 60 * 60) + EXTRACT (SECOND FROM SYSTIMESTAMP), 3)*1000 FROM DUAL));

INSERT INTO HELP_SUPPORT_TEAM(TYPE, VALUE, STATUS, CREATED_BY, CREATED_AT)
VALUES('EMAIL', 'support.sbiepay@sbi.co.in', 'Active', 'SBI ePay',
(SELECT ROUND(((TRUNC(SYSTIMESTAMP,'MI')-DATE '1970-01-01') * 24 * 60 * 60) + EXTRACT (SECOND FROM SYSTIMESTAMP), 3)*1000 FROM DUAL));

commit;

--changeset Subhra:2
ALTER TABLE HELP_SUPPORT_TEAM ADD VALUE_TEMP CLOB;
UPDATE HELP_SUPPORT_TEAM SET VALUE_TEMP = VALUE;
ALTER TABLE HELP_SUPPORT_TEAM DROP COLUMN VALUE;
ALTER TABLE HELP_SUPPORT_TEAM RENAME COLUMN VALUE_TEMP TO VALUE;

--changeset Subhra:3
INSERT INTO HELP_SUPPORT_TEAM(TYPE, VALUE, STATUS, CREATED_BY, CREATED_AT)
VALUES('IMPORTANT NOTICE', '["New Feature : Check the health status of all payment channels in one place.",
"New Feature : Real-time transaction tracking now available in Dashboard.",
"Feature Enhancement: Faster settlement reports with detailed insights.",
"Notice: RBI mandates additional authentication for high-value transactions.", "Notice: NPCI announces new limits 5000 Rs. for UPI transactions."]',
 'Active', 'SBI ePay',
(SELECT ROUND(((TRUNC(SYSTIMESTAMP,'MI')-DATE '1970-01-01') * 24 * 60 * 60) + EXTRACT (SECOND FROM SYSTIMESTAMP), 3)*1000 FROM DUAL));
