Cannot resolve method 'thenReturn(IMap<String, Object>)' 

   @BeforeEach
    void setUp() {
        when(hazelcastInstance.getMap(MAP_NAME)).thenReturn(mockMap);
        when(hazelcastInstance.getMap(MAP_NAME)).thenReturn(jsonMockMap);
    }
