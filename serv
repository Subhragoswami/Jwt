 private static String getFileName(String reportName) {
        return ReportingConstant.REPORT_ROOT_FOLDER + StringEscapeUtils.escapeJava(reportName + "_" + LocalDateTime.now().format(FORMATTER_DD_MM_YY_HH_MM_SS) + ".csv");
    }

here how to avoid concondination like this and use stringutils.
