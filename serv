   void validateMerchantUser(OtpGenerationRequest otpGenerationRequest) {
        UserStatus userStatus = RequestType.CHANGE_PASSWORD.getName().equals(otpGenerationRequest.getRequestType())
                ? UserStatus.EXPIRED
                : UserStatus.ACTIVE;
        boolean merchantUserExists = otpManagementDao.isValidMerchantUserExists(otpGenerationRequest.getUserName(), userStatus);
        if (!merchantUserExists) {
            addError("Active Merchant User", ErrorConstants.NOT_FOUND_ERROR_CODE, ErrorConstants.NOT_FOUND_ERROR_MESSAGE);
        }
        throwIfErrors();
    }

here I need to add logic like if otpGenerationRequest.getRequestType() is RESET_PASSWORD then we need to accept user status either it should be ACTIVE or EXPIRED 
