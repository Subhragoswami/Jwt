Caused by: java.lang.IllegalArgumentException: org.hibernate.query.SemanticException: Could not interpret path expression 'currentTimeMillis'

    @Query("Select o from OtpManagement o where o.requestId = :requestId and (o.otpCode LIKE 'E%' or o.otpCode LIKE 'S%') and o.expiryTime > currentTimeMillis  order by o.createdAt DESC")
    List<OtpManagement> findLatestTwoOtpByRequestId(
            UUID requestId, Pageable pageable, Long currentTimeMillis);
